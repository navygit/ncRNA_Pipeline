Genefinder.pm - modified the runnable to point at the altered config file.
gene_descriptions - parses gene description out of the gff file and writes them
as sql statements to load into the database.
genes_prediction_transcripts.pl - reads the genes from one database and writes
them into a second db as prediction transripts / exons for use in the
BlastGenscanPep step.
SGD.pm - module modified from the wormbase gff parsing module.
gene_store.pl points the SDG module at the gff file and runs tha appropriate
methods.
SGDConf.pm  configuration for the SGD module
